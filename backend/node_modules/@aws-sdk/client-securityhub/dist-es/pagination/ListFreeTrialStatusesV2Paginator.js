import { createPaginator } from "@smithy/core";
import { ListFreeTrialStatusesV2Command, } from "../commands/ListFreeTrialStatusesV2Command";
import { SecurityHubClient } from "../SecurityHubClient";
export const paginateListFreeTrialStatusesV2 = createPaginator(SecurityHubClient, ListFreeTrialStatusesV2Command, "NextToken", "NextToken", "MaxResults");
